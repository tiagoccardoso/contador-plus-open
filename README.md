
# Contador+ (v0.3)

Agora com:
- **OpenAI real** via `dio` (usa `.env`)
- **Exportar .ics** do vencimento (compartilhar arquivo)

## Rodando
1. `cp .env.example .env` e defina `OPENAI_API_KEY`
2. `flutter pub get`
3. Se faltar `android/`: `flutter create .`
4. `flutter run`

> Troque o modelo/endpoint no `.env` se quiser.
