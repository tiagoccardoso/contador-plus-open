// PATCH para main.dart
// Adicione o import:
//   import 'src/features/deputados/deputado_detail_screen.dart';
//
// E dentro do ShellRoute(routes: [...]) inclua esta rota:
//
//   GoRoute(
//     path: '/deputados/:id',
//     builder: (ctx, st) {
//       final id = int.parse(st.pathParameters['id']!);
//       final nome = (st.extra is Map) ? (st.extra as Map)['nome'] as String? : null;
//       return DeputadoDetailScreen(id: id, nome: nome);
//     },
//   ),
